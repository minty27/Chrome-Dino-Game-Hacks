// Enter the line below first into the console  
Runner().tRex.groundYPos = 10

// Enter this line below afterwards
// If you want to set the speed higher, do it in intervals. Start with 100, then 500, the 1000
Runner.instance_.setSpeed(100)
